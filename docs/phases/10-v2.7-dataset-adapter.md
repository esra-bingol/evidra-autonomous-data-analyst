# Aşama 10.7 — İkinci dataset adapter (V2.7)

**Proje:** Evidra  
**Önceki:** [10-v2.6-report.md](10-v2.6-report.md)  
**Sonraki:** yok (V2.8 açılmadı)

UCI Online Retail II **şeması**. Ham Kaggle dosyası repoda yok; CI `data/fixtures/retail_line_items.csv` ile geçer.

---

## Amaç

Yeni bir `calculate_uci_revenue` tool’u değil: raw invoice satırı → mevcut role table’ın okuduğu kanonik çerçeve.

```text
InvoiceDate  → time
Quantity     → metric (quantity)
amount       → metric (sales)   # adapter: qty × unit price, tool değil
Country      → dimension
Invoice      → id (order)
Customer ID  → id (customer)
```

Eksik capability (bilinçli): `delivery_analysis`, `multi_table_join`, `retention_analysis` (unique customer panel yok), `causal_analysis`. Profit kolonu yok → abstain.

---

## Adapter

`analysis/retail.py`: şema tespiti + `amount` türevi + iptal/negatif satır düşürme. Evidence `canonicalize`. Closed tool set değişmez.

İsteğe bağlı gerçek Excel: `data/raw/online_retail_ii.xlsx` (gitignore). UI upload.

---

## Eval (`evals/uci_retail.json`)

10 süreç case: metric/time, country segment, volume/value, canonicalize, completeness+reviewer, delivery/profit/join/retention abstain, ranking. Sayısal city golden yok.

V1 golden + Olist + adversarial + adaptive + reviewer regresyonu durur.

---

## CANNOT

NYC Taxi (XOR), V2.8, UCI-specific analysis tool, ham dosyayı commit.

---

## Görevler

- [x] Schema / role incelemesi
- [x] Minimal adapter
- [x] 10 process case
- [x] Regression
